import { connect } from "react-redux";
import "./mainPage.css";

const MainPage = () => {
    return (
        <>
            <div className="mainPage">4</div>
        </>
    );
};

export default connect(null, null)(MainPage);
